const initialState = {
  answers: {},
}

const answersReducer = (state = initialState, action) => {
  switch (action.case) {
    default: {
      return state
    }
  }
}

export default answersReducer
